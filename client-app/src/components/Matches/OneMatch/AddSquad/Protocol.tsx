import { SquadListType } from "../../types"

interface PropsType {
    squad: SquadListType
} 

const Protocol = ({squad} : PropsType) => {
    return <div>
        
    </div>
}

export default Protocol