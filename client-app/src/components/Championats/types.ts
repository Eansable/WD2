export interface ChampionatType {
    id: number,
    name: string,
    yearString?: string,
    startDate?: Date,
    endDate?: Date,
    playersCount?: number,
    championatFormat?: number,
    isActive?: boolean
}